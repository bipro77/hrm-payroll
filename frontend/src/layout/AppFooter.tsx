import { CFooter } from "@coreui/react";

const AppFooter = () => {
  return (
    <CFooter className="px-4">
      <span>HRM Payroll</span>
      <span className="ms-auto">Employee management</span>
    </CFooter>
  );
};

export default AppFooter;
